# allowme.md — 둠챠 표준 작업 플레이북

> 이 문서는 "2026-06-20 리팩토링 + README 작업"이 마음에 들어서, **앞으로 비슷한 작업을 같은 방식으로** 진행해 달라고 남긴 지시서다.
> 새 작업을 시작할 때 이 파일을 그대로 프롬프트로 붙여넣거나 "allowme.md 방식으로 해줘" 라고만 해도 된다.

---

## 0. 한 줄 요약

**"검증된 코드는 절대 눈으로만 믿지 말고, 실행 결과의 바이트 동일성으로 증명하면서 바꾼다. 브랜치 따고, 골든 잡고, 결정론적으로 고치고, 비교하고, PR로 설명한다."**

---

## 1. 핵심 원칙 (반드시 지킬 것)

1. **main 직접 수정 금지.** 항상 새 브랜치(`refactor/...`, `feature/...`, `fix/...`)에서 작업하고 **PR로** 올린다.
2. **동작 보존이 최우선.** 특히 백테스트·트레이딩 코드는 수치 하나만 바뀌어도 안 된다. "로직 안 건드림"은 말이 아니라 **증거(출력 동일성)** 로 보인다.
3. **눈으로 추측하지 말고 도구로 확인한다.** 파일 구조는 `ast`로, 의존성은 호출 그래프로, 동일성은 `diff`로.
4. **결정론적으로 변환한다.** 손으로 5천 줄을 자르지 말고, **추출 스크립트**를 만들어 재현 가능하게 한다 (`tools/`에 남긴다).
5. **주석은 자산이다.** 특히 "주석판(◆ 해설)"은 라인범위 단위로 **verbatim 보존**한다. `ast.unparse`처럼 주석을 날리는 방식 금지.
6. **함정을 먼저 찾는다.** 노트북 셀 잔재(중간 상수 재정의), 런타임 변이 전역, multiprocessing 직렬화, 파일 끝 쓰레기 줄 등은 흔하다. 작업 전에 스캔한다.
7. **막판에 사용자에게 방향을 묻는다.** 위험·범위·전달 방식(깊이/검증/브랜치)은 시작 전에 2~4개 선택지로 확정한다.

---

## 2. 표준 절차 (리팩토링/대규모 변경)

### STEP 1 — 정렬(질문)
시작 전에 사용자에게 확인한다 (보통 3개):
- **범위**: 모듈 분리만(최대 안전) / 분리+정리 / 전면 재설계
- **검증**: 실행 결과(CSV 등) 동일성 비교 / import·AST 수준만
- **전달**: 새 브랜치 + PR / main 직접

### STEP 2 — 환경·결합도 점검
- 의존성·런타임(파이썬 버전, 패키지)·네트워크 접근 가능 여부 확인
- `ast`로 top-level 노드 전수 인벤토리 (함수/상수/실행부/별칭)
- **변이 전역**(런타임에 재대입되는 이름), **중간 재정의 상수**, **모듈레벨 실행부** 식별
- 파일 끝/중간 쓰레기·SyntaxError 여부 확인

### STEP 3 — 골든 레퍼런스 확보 ⭐
- **바꾸기 전에** 원본을 1회 완주시켜 산출물(CSV/로그)을 **골든으로 잠근다**.
- 네트워크 다운로드가 있으면 **`requests.get` 캐시 하니스**로 1회만 받고 재사용 (전/후 실행 모두 캐시 공유).
- 원본이 실행조차 안 되면(쓰레기 줄 등) 최소 수정한 clean본으로 골든을 잡고, 그 수정도 PR에 포함·명시.

### STEP 4 — 결정론적 변환
- 모듈 경계는 **의존성 레이어**로 잡아 **순환 없게** (호출 그래프로 검증, 순환 시 함수 재배치/저수준 `utils` 분리).
- 추출은 **라인범위 단위**(노드 앞 주석 동반) → 코드·주석 verbatim.
- 변이 전역은 `config.X`로 접근, `config.__all__`에서 제외해 **누락 시 NameError로 큰 소리로** 실패하게.
- 중간 재정의 상수는 **최종 실효값을 단일 소스(config)로 통합** (multiprocessing 워커가 부모 변경 못 보는 문제까지 해결).
- 변환기는 `tools/`에 **상대경로**로 남긴다. 생성물 상단에 "자동 추출, 직접 수정 금지, 재생성 명령" 표기.

### STEP 5 — 검증 (증명)
- 변환판을 **같은 캐시**로 완주 → 골든과 **전 파일 `diff -q` 바이트 비교**.
- 다르면 원인을 끝까지 추적(보통 변이 상수/직렬화/순서). **전부 일치할 때까지** 반복.
- 컴파일(`py_compile`) + import 그래프(실행 없이) 사전 점검도 한다.

### STEP 6 — 문서화 & 전달
- **README**: 한눈에 보기 표 → 전략/구조 개요 → 실행법(+환경변수) → 산출물 → 구조도 → 한계. **사람이 위→아래로 읽히게**, 표·코드블록·이모지로 구조화.
- 코드 주석에 있던 핵심 수치·경고·미해결 이슈를 README에 빠짐없이 반영.
- **PR**: 무엇을/왜 → 구조도 → **검증 결과 표(동일성)** → 핵심 보존 처리 → 버그 수정 → 리뷰 포인트.
- 커밋/PR 말미 서명 규칙 준수.

---

## 3. README 작성 톤

- 한국어 기본, 표/코드블록/이모지 적극 활용.
- 첫 화면에서 "이게 뭐고 성과가 어떤지" 즉시 파악되게 (요약 표 먼저).
- 추측 금지 — 실제 코드/주석/실행에서 확인한 사실만. 불확실하면 명시.
- 위험·한계·"투자 조언 아님" 같은 면책은 꼭 남긴다.

---

## 4. 안티패턴 (하지 말 것)

- ❌ 골든 없이 바로 고치고 "아마 똑같을 것" 이라고 말하기
- ❌ 손으로 대용량 파일 잘라 옮기기 (재현 불가, 실수 유발)
- ❌ `ast.unparse`/포매터로 주석·서식 날리기
- ❌ 변이 전역을 `from config import *`로 복사 (런타임 변경 전파 안 됨)
- ❌ 검증을 "import 되니 OK"로 끝내기 (수치 회귀 못 잡음)
- ❌ main에 직접 커밋, 또는 PR 설명 없이 머지

---

*작성: 2026-06-20 / 기준 작업: PR #1 (5,271줄 단일 파일 → `smc/` 패키지, 22개 CSV 바이트 동일 검증)*
